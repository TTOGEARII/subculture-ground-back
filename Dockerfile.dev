# NestJS 백엔드 개발용 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 네이티브 모듈 빌드를 위한 빌드 도구 설치
RUN apk add --no-cache python3 make g++

# 의존성 설치
COPY package*.json ./
RUN npm install --no-audit --no-fund

# 개발 서버 실행 (볼륨 마운트로 코드 변경사항 반영)
CMD ["npm", "run", "start:dev"]
